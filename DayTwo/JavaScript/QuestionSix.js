

let isPalindrome = (int number) => { 
        newNumber = Integer.toString(number);
        reverse = "";

        for(let count = 0; count < array.length; count++){
            reverse = newNumber.charAt(count) + reverse;        
         }

        reverse = Integer.parseInt(reverse);
        if(reverse == number) return true
    return false
}


